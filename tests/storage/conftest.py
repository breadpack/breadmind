"""Storage-layer test fixtures.

The ``test_db`` fixture has been promoted to ``tests/conftest.py`` so
that other test suites (e.g. ``tests/flow``) can reuse it without
duplication. This module is kept as a placeholder and may host
storage-specific fixtures in the future.
"""
from __future__ import annotations
