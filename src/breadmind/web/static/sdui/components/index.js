// src/breadmind/web/static/sdui/components/index.js
// Component registry — populated by Tasks 18-20.
// Each module exports a default object mapping type name → render function.
//
// Render function signature: (component, render, ctx) => VNode
//   - component: { type, id, props, children }
//   - render: recursive renderer for children (use `render(child, ctx)`)
//   - ctx: context { dispatch, requestView, formValues?, setFormValue? }

const registry = {};

export default registry;
